[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "rust-numeric"
version = "0.1.0"
description = "Python bindings for the Numerical Computing Rust examples."
requires-python = ">=3.11"

[tool.maturin]
module-name = "rust_numeric"
